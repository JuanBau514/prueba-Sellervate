# P10 · Decisiones y README

## Instrucción del usuario

> ya he verificado y hecho merge ahora vamos a trabajar sobre los ulitmos 2 p9 y p10 nesesito que ajustes estoo especialmente porque ya solamentte falta estos 2 apartados

Respuestas del autor durante la sesión:
- Horas: «el tiempo total con los intervalos corresponde a 6 horas para los requerimientos técnicos y el pipeline me he demorado 30 min en analizar el problema y tener un pipeline».
- Ramas: dos ramas en cadena.
- Clon limpio: ejecutarlo.
- Sección AI de DECISIONS.md: borrador marcado para su confirmación.

## Prompt independiente aplicado

Crear `docs/decisions` desde `chore/states-polish`. README bilingüe y equivalente: requisitos, `supabase start` → `db reset` → `seed` → `dev`, usuarios de demo y cambio de rol, `authz-check`, horas reales, qué se probaría primero, clon limpio medido de verdad, estado y lista de entrega. `DECISIONS.md` en inglés, máximo dos páginas: Product, Architecture, AI y Status. No atribuir al autor juicios que no escribió.

## Evidencia y decisiones

- Herramienta: Claude Code (Opus 5.5).
- **Clon limpio:** `git clone` local de la rama, `command npm ci` (el alias del autor redirige npm a pnpm; se usó el binario real solo aquí, con su permiso), `db:start`, `.env.local` desde `db:status`, `db:reset`, `seed`, `next dev` en el puerto 3200 y `authz-check` contra él. Total **65 s** (npm ci 5 s, reset 31 s). Cachés calientes: no representa una máquina nueva. El clon se borró después. El reset borró una revisión de prueba del autor, como se le avisó.
- **Repositorio público** comprobado sin sesión (API de GitHub: `private: false`, `default_branch: main`).
- **README reescrito** en su forma final en lugar de acumular la narrativa por problema; el detalle histórico sigue en `ai-logs/` y `02-pipeline.md`. Se recuperó la mención de «sin starter kit» que exige el PDF.
- **DECISIONS.md**: ~1.000 palabras. Correcciones del autor tomadas de los registros (normalización P1, ramas separadas, sin dependencias, captura de la cola). «A prompt I am proud of» y «the strongest objection» van marcados como borrador.
- **Horas:** se registran tal como las declaró el autor (6 h 30 min). Se deja constancia de que el brief fija seis horas incluyendo la planificación; la decisión sobre cómo declararlo es del autor.

## Comprobaciones ejecutadas

- Clon limpio completo con `authz-check` 29/29 en el clon.
- Enlaces del README y del checklist a archivos existentes.
- Sin cambios de código en P10.

## Pendiente del autor

Confirmar o reescribir los dos apartados en borrador de DECISIONS.md; revisar e integrar P9b y después P10; comprobar en GitHub el texto de revisión de cada PR; confirmar la fecha de recepción; poner enlace y horas en la propuesta de Upwork; si puede, repetir el clon limpio sobre `main` final.

## Seguimiento en el mismo PR

> ya he he creado la pull request pero nesesito hacer que las desiciones sean en ingles y es pannol ten en cuenta qu elo que hemos construioo all pipeliine de desarrollo problemas y el pipeline de desarrollo final fue en busca de construir un producto y no un sistema generico de calificacion de comentatrios para clientes que vayan a usar esta plataforma asi mismo esto debe de estar dentro del archivo, en el readme debe de existir una guia de comandos de instraacioon junto al docker la base de datos requerimientos etc , y en el desiciooons debe de estar en inglles y español asi como en ell readme

- P9b ya estaba integrado (PR #10, `a199b2d`); el PR de P10 estaba abierto. Commit de seguimiento en `docs/decisions`, sin reescribir el anterior.
- `DECISIONS.md` en inglés y español con contenido equivalente, y un apartado inicial nuevo: el análisis y el pipeline buscaban un producto para el loop de calidad de Sellervate, no un sistema genérico de calificación de comentarios. La sección en inglés se mantiene en 956 palabras (dos páginas); la española es su equivalente.
- README: la «Puesta en marcha» pasa a ser una guía de instalación en seis pasos, en los dos idiomas: tabla de requisitos con versiones probadas (Node 24.14, npm 11.11, Docker Desktop 29.7 con 8 GB, Supabase CLI 2.117), cómo instalar Docker y Node, instalación, generación automática de `.env.local`, base de datos y seed, comandos útiles con puertos y Studio, y solución de problemas. La descripción inicial deja claro que no es un calificador genérico.
- Verificado: el comando que genera `.env.local` produce un archivo idéntico al que usa la app (probado en el scratchpad); los flags `--project-id`, `--no-backup` y `migration up --local` existen en el CLI instalado; ~8,6 GB de imágenes de Supabase medidos con `docker images`.
- Sin cambios de código.
